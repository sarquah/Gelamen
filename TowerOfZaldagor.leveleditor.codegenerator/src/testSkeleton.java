import Leveleditor.*;
/**
 * 
 * @author Sarquah
 * @info Test class to check if JET is compiled correct
 */
public class testSkeleton 
{
	public void initialize(Act act)
	{
		EventRoom room = null;
		for (Item item : room.getItems()) {
			if (room.getEvents().indexOf(item)==room.getEvents().size()-1) {
				
			}
		}
	}
	
	public String generate(Object argument) {
		return "";
	}
}
